{
  "name": "mdx-live",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna version --no-push --conventional-commits && lerna publish from-git"
  },
  "devDependencies": {
    "lerna": "^3.13.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
